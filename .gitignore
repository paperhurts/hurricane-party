# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# rust build artifacts, anywhere in the tree (not just src-tauri)
target/

# bundled sidecars (large, fetched by tools/fetch-sidecars.ps1)
src-tauri/binaries/

# screenshots dropped in by hand
.sid/

# a browser automation scratch folder, never source
.playwright-mcp/

# Claude Code: the skills are shared, the local settings are not.
# settings.local.json holds per-machine choices (which plugins this checkout
# has enabled) and would fight between machines if it were tracked.
.claude/settings.local.json
